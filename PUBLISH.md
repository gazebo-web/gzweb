## Versioning

We follow [semantic versioning](https://semver.org).

## NPM release process

1. Log into npm
  ```
  npm login
  ```

2. Increase the version number in `package.json`

3. Build gzweb
  ```
  npm run build
  ```

4. Publish to npm

  ```
  npm publlsh
  ```
